# this rubber run should fail, an invalid bibtex database being included.
if $python ../rubber.py $VERBOSE doc 2> tmp; then
    cat tmp
    exit 1
fi
rm tmp
rm doc.aux doc.dvi doc.log doc.bbl doc.bcf doc.blg doc.run.xml
